<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cinesquare.cinesquare.common.mapper.MovieMapper">

<select id="searchMovie" parameterType="String" resultType="MovieVO">
    <![CDATA[
        /* MovieMapper.searchMovie */
        SELECT movieCd
            , movieNm
            , janres
            , directors
            , actors
            , nations
            , openDt
            , showTm
        FROM tb_movie
        WHERE movieNm LIKE CONCAT('%', #{param}, '%')
             OR directors LIKE CONCAT('%', #{param}, '%')
             OR actors LIKE CONCAT('%', #{param}, '%')
    ]]>
</select>
</mapper>